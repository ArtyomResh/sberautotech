version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: sberautotech
      POSTGRES_USER: user
      POSTGRES_PASSWORD: admin
    ports:
      - 5432:5432
