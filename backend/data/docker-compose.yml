services:
  db:
    image: postgres:alpine
    restart: always
    environment:
      POSTGRES_DB: sberautotech
      POSTGRES_USER: user
      POSTGRES_PASSWORD: admin
    ports:
      - 5433:5432
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
