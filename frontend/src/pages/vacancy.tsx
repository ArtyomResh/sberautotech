import React from 'react'
import Layout from '../components/layout'

const vacancy = () => {
    return (
        <Layout theme={{ mode: 'dark', logoColor: '#040A0A' }} pageNumber={3}>
            TestString
        </Layout>
    )
}

export default vacancy
